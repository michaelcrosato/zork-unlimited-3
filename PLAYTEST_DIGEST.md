# Playtest Digest

Auto-generated by `npm run playtest:consolidate`. Newest window on top. The coding agent reads the top section at the start of each planning window.

_No window consolidated yet — the parallel `npm run playtest:loop` accrues sessions into `playtest-feedback/sessions.jsonl`, and the 24h consolidation prepends a dated section here._
